const config = {
    "apiPrefix": "http://localhost:8080",
    "serverPort": "8080",
    "db": {
        "name": "lists",
        "host": "localhost",
        "port": 27017
    }
};

export default config;